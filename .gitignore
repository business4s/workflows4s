# IDE files / BSP / LSP caches
.idea
.bsp
.vscode
.metals
.bloop
.scala-build

# sbt
target
**/metals.sbt

# JS
dist
node_modules
.vite

# Ignore package-lock.json except for workflows4s-web-ui (needed for npm ci)
package-lock.json
!workflows4s-web-ui/package-lock.json

# Kiro specs (local development)
.kiro/
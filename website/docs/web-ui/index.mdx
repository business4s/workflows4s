# Web UI & API


Workflows4s comes with an experimental UI that allows to inspect and interact with the workflows.
It's powered by HTTP API that can be used on its own.
Check the [demo](demo) to see how it works in practice.

:::warning

Neither UI nor API provide authentication or authorization support.
Until this changes, we advise protecting it through external means.

:::

## Getting Started

```scala file=./main/scala/workflows4s/example/docs/web/GettingStarted.scala start=doc_start end=doc_end
```